// Entry point. Each module wires up its own bit of the page and bails early
// if the markup it cares about isn't present, so this is safe to load everywhere.
import './navigation.js';
import './disclosure.js';
import './contact.js';
